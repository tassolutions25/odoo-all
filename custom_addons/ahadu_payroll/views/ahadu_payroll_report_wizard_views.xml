<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_ahadu_payroll_custom_report_wizard_form" model="ir.ui.view">
        <field name="name">ahadu.payroll.custom.report.wizard.form</field>
        <field name="model">ahadu.payroll.custom.report</field>
        <field name="arch" type="xml">
            <form string="Custom Payroll Report">
                <group>
                    <group string="Period">
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                </group>
                <group string="Filters (Optional)">
                    <field name="pay_group_ids" widget="many2many_tags" options="{'no_create': True}"/>
                    <field name="branch_ids" widget="many2many_tags" options="{'no_create': True}"/>
                    <field name="department_ids" widget="many2many_tags" options="{'no_create': True}"/>
                </group>
                <footer>
                    <button name="action_generate_excel" string="Generate Excel" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_ahadu_payroll_custom_report_wizard" model="ir.actions.act_window">
        <field name="name">Custom Payroll Report</field>
        <field name="res_model">ahadu.payroll.custom.report</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Menu for custom wizard -->
    <menuitem id="menu_report_custom_payroll" 
              name="Custom Payroll Report" 
              parent="ahadu_payroll.menu_ahadu_reports_root" 
              action="action_ahadu_payroll_custom_report_wizard" 
              sequence="40" />
</odoo>