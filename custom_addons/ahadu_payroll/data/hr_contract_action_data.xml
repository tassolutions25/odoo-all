<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="action_set_ahadu_structure_fixed" model="ir.actions.server">
            <field name="name">Set Default Ahadu Structure (Fixed)</field>
            <field name="model_id" ref="hr_contract.model_hr_contract"/>
            <field name="binding_model_id" ref="hr_contract.model_hr_contract"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">
structure = env.ref('ahadu_payroll.structure_ahadu_monthly', raise_if_not_found=False)
if structure:
    # Use struct_id as identified by user
    records.write({'struct_id': structure.id})
            </field>
        </record>
    </data>
</odoo>
