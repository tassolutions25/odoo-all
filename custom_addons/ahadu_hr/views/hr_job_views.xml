<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <record id="view_job_tree_extended" model="ir.ui.view">
        <field name="name">hr.job.tree.extended</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_hr_job_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='no_of_employee']" position="after">
                <field name="grade_id" optional="show"/>
                <field name="division_id" optional="show"/>
                <field name="status" widget="badge" decoration-success="status == 'filled'" decoration-warning="status == 'vacant'" decoration-danger="status == 'frozen'"/>
            </xpath>
        </field>
    </record>

    <record id="view_job_form_extended" model="ir.ui.view">
        <field name="name">hr.job.form.extended</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_hr_job_form"/>
        <field name="arch" type="xml">
            
            <xpath expr="//sheet" position="before">
                <header>
                    <field name="status" widget="statusbar" statusbar_visible="vacant,filled,frozen"/>
                </header>
            </xpath>
            
            <xpath expr="//field[@name='department_id']" position="after">
                <field name="ahadu_employee_type_ids" widget="many2many_tags" string="Employee Type"/>
                <field name="division_id"/>
                <field name="grade_id"/>
                <field name="cost_center_id"/>
            </xpath>

        </field>
    </record>

</odoo>